import { resolve } from "path"

export const DOWNLOADS_PATH = resolve(
  __dirname,
  "..",
  "..",
  "..",
  "output",
  "files",
)
export const SCREENSHOTS_PATH = resolve(
  __dirname,
  "..",
  "..",
  "..",
  "output",
  "screenshots",
)
